[package]
name = "modbus-rtu"
version = "0.1.1"
edition = "2021"
authors = ["Park JiSoo <cherryjeesoo@gmail.com>"]
description = "Standard Modbus RTU protocols"
license = "MIT"

[dependencies]
heapless = "0.8.0"

[features]
default = ["slave", "no_std"]
no_std = []
master = []
slave = []